from MrPickles.CommandHandlers.BaseHandler import BaseHandler

# if the time between keepalive packets is > .5 sec we
# need to kill the motor. if it resumes
# start doing the things with the stuff

class KeepAlive(BaseHandler):
	COMMAND_ID=1000
	
	def __init_(self):
		pass
		
	def parser(self,boogz):
		pass

	
